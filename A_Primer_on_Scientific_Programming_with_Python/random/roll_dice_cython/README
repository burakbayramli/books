roll_dice.py tests versions roll_dice[1-3].py in the parent directory with
similar implementations using Cython to speed up the code. That is,
plain Python implementation of the Monte Carlo simulation is compared
to two vectorized versions and two Cython versions.