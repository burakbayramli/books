Stan Blank
./PyExpJulia.py   shows a small panel, hard to stop   
./PyInverseJulia.py shows nothing, uses complex inverses, hard to stop
./PyJulia.py      shows Julia set all at once, uses forward iteration

Note. None of these are a translation of the Kauffman algorithm as in the
Hippocket lessons. 

