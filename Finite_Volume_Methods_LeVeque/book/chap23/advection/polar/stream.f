c
c     =================================================
      function stream(x,y)
c     =================================================
c
c     # stream function in physical space (x,y)

      implicit double precision (a-h,o-z)

      stream = y
c
      return
      end
