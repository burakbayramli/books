function [Ux, Uy, PR] = INScylinderIC2D(x, y, time, nu)

% function [Ux, Uy, PR] = INScylinderIC2D(x, y, time, nu)
% Purpose: evaluate solution for cylinder flow 

Ux = zeros(size(x)); Uy = zeros(size(x)); PR = zeros(size(x));
return

